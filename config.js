/* ============================================================
   Pabloch Tech — your settings.

   THIS FILE IS YOURS. Nothing regenerates it, so the Web3Forms
   key and the handles below survive every future rewrite of
   index.html. If a new index.html ever lands in this folder,
   leave this file exactly where it is and everything keeps working.

   Handles go in WITHOUT the @. Blank one out ("") and that button
   disappears from the contact block, the footer and the mobile menu.
   ============================================================ */
window.PABLOCH_CONFIG = {

  /* Free key from web3forms.com. Public by design — it ships in the
     HTML of every static site that uses one, so it is not a secret.
     Rotate it at web3forms.com if it is ever abused.
     NOTE: a key delivers to the address it was created with. This one
     was created for the Gmail address, so enquiries still arrive there.
     To move them to hello@pablochtech.com, generate a NEW key using
     that address and paste it here. */
  WEB3FORMS_KEY : "d85e0497-4eae-40f9-af9d-3630bf80d328",

  EMAIL         : "hello@pablochtech.com",
  TELEGRAM      : "pablochtech",
  INSTAGRAM     : "pablochtech",
  FACEBOOK      : "pablochtech"
};
